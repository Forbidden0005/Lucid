<Page
    x:Class="Lucid.Views.TimelinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lucid.Views"
    xmlns:vm="using:Lucid.ViewModels"
    Background="{StaticResource AppBackgroundBrush}">

    <!--
        TimelinePage — Operational Timeline
        ═══════════════════════════════════════════════════════════════════════════

        Layout: fixed header (title + count) → filter chip strip → scrollable
        event list. The outer Grid pins the top two rows and lets Row 2 fill
        the remaining height so the list scrolls independently.

        List structure:
          FlatItems is a mixed ObservableCollection<object> of two types:
            TimelineGroupHeaderViewModel  — section divider (Now / Last 15m / Today / Earlier)
            TimelineEventViewModel        — event card

          TimelineItemTemplateSelector routes each item to the correct DataTemplate.
          This avoids CollectionViewSource group-binding complexity while keeping the
          ListView virtualised across the full flat list.

        Event card layout (per card):
          ┌── 3px accent stripe ──┬─────────────────────────────────────────────┐
          │                       │ [Glyph]  Title                [Tag]  Time ▼ │
          │                       │          Process: Chrome, discord             │
          │                       │ ─────────────────────────────────────────── │ (expanded)
          │                       │ Detail paragraph…                            │
          │                       │ Timestamp: h:mm:ss tt                        │
          └───────────────────────┴─────────────────────────────────────────────┘

        Animations:
          EntranceThemeTransition on the ListView gives a smooth slide-in when the
          page first loads. Individual card expand/collapse uses Visibility toggling
          (no custom Storyboard required).
    -->

    <Page.Resources>

        <!-- ── Template selector ──────────────────────────────────────────── -->
        <local:TimelineItemTemplateSelector x:Key="TimelineTemplateSelector">

            <!-- Section header template -->
            <local:TimelineItemTemplateSelector.HeaderTemplate>
                <DataTemplate x:DataType="vm:TimelineGroupHeaderViewModel">
                    <Grid Padding="4,20,4,8">
                        <StackPanel Orientation="Horizontal" Spacing="10"
                                    VerticalAlignment="Center">

                            <!-- Section label (all-caps, spaced) -->
                            <TextBlock Text="{x:Bind Label}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       CharacterSpacing="75"
                                       Foreground="{StaticResource SecondaryTextBrush}" />

                            <!-- Event count badge -->
                            <Border Background="#1AFFFFFF"
                                    CornerRadius="10"
                                    Padding="7,1">
                                <TextBlock Text="{x:Bind CountText}"
                                           FontSize="11"
                                           Foreground="{StaticResource SecondaryTextBrush}" />
                            </Border>
                        </StackPanel>

                        <!-- Hairline separator -->
                        <Rectangle Height="1"
                                   Fill="#18FFFFFF"
                                   VerticalAlignment="Bottom"
                                   Margin="0,0,0,0" />
                    </Grid>
                </DataTemplate>
            </local:TimelineItemTemplateSelector.HeaderTemplate>

            <!-- Event card template -->
            <local:TimelineItemTemplateSelector.EventTemplate>
                <DataTemplate x:DataType="vm:TimelineEventViewModel">
                    <Border Background="{StaticResource AppSurfaceBrush}"
                            CornerRadius="10"
                            BorderBrush="{StaticResource BorderSubtleBrush}"
                            BorderThickness="1"
                            Margin="0,0,0,6">

                        <!-- Inline CompositeTransform for hover scale (per-card, not shared) -->
                        <Border.RenderTransform>
                            <CompositeTransform />
                        </Border.RenderTransform>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- ── Severity accent stripe ─────────────────── -->
                            <Border Grid.Column="0"
                                    Background="{x:Bind AccentBrush}"
                                    CornerRadius="10,0,0,10" />

                            <!-- ── Card content ───────────────────────────── -->
                            <StackPanel Grid.Column="1"
                                        Padding="14,12,14,12"
                                        Spacing="0">

                                <!-- Header row: glyph + title + meta -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Glyph container -->
                                    <Border Grid.Column="0"
                                            Width="32" Height="32"
                                            CornerRadius="8"
                                            Background="{x:Bind AccentBackground}"
                                            Margin="0,0,12,0"
                                            VerticalAlignment="Center">
                                        <FontIcon Glyph="{x:Bind Glyph}"
                                                  FontFamily="Segoe MDL2 Assets"
                                                  FontSize="15"
                                                  Foreground="{x:Bind AccentBrush}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                                    </Border>

                                    <!-- Title -->
                                    <TextBlock Grid.Column="1"
                                               Text="{x:Bind Title}"
                                               Style="{StaticResource CardTitleStyle}"
                                               VerticalAlignment="Center"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis" />

                                    <!-- Right meta: type chip + time + expand toggle -->
                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal"
                                                Spacing="8"
                                                VerticalAlignment="Center"
                                                Margin="12,0,0,0">

                                        <!-- Type label chip -->
                                        <Border Background="{x:Bind AccentBackground}"
                                                CornerRadius="10"
                                                Padding="8,3">
                                            <TextBlock Text="{x:Bind TypeLabel}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="{x:Bind AccentBrush}" />
                                        </Border>

                                        <!-- Relative time -->
                                        <TextBlock Text="{x:Bind RelativeTime}"
                                                   Style="{StaticResource CaptionTextStyle}"
                                                   VerticalAlignment="Center"
                                                   MinWidth="54" />

                                        <!-- Expand toggle (hidden when no detail or processes) -->
                                        <Button Command="{x:Bind ToggleExpandCommand}"
                                                Visibility="{x:Bind ExpandToggleVisibility}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Width="28" Height="28"
                                                Padding="0"
                                                VerticalAlignment="Center"
                                                ToolTipService.ToolTip="Show details">
                                            <FontIcon Glyph="{x:Bind ExpandGlyph, Mode=OneWay}"
                                                      FontFamily="Segoe MDL2 Assets"
                                                      FontSize="11"
                                                      Foreground="{StaticResource SecondaryTextBrush}" />
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- Process attribution line (always visible when present) -->
                                <StackPanel Orientation="Horizontal"
                                            Visibility="{x:Bind ProcessesVisibility}"
                                            Margin="44,6,0,0"
                                            Spacing="6">
                                    <FontIcon Glyph="&#xE9F5;"
                                              FontFamily="Segoe MDL2 Assets"
                                              FontSize="11"
                                              Foreground="{StaticResource SecondaryTextBrush}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{x:Bind ProcessesText}"
                                               FontSize="12"
                                               Foreground="{StaticResource SecondaryTextBrush}"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <!-- Expanded detail panel -->
                                <Border Visibility="{x:Bind ExpandedContentVisibility, Mode=OneWay}"
                                        Background="#0AFFFFFF"
                                        CornerRadius="6"
                                        Padding="12,10"
                                        Margin="0,10,0,0">
                                    <StackPanel Spacing="8">

                                        <!-- Detail text -->
                                        <TextBlock Text="{x:Bind Detail}"
                                                   Style="{StaticResource BodyTextStyle}"
                                                   TextWrapping="Wrap"
                                                   IsTextSelectionEnabled="True" />

                                        <!-- Timestamp footer -->
                                        <TextBlock Text="{x:Bind TimestampText}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource SecondaryTextBrush}"
                                                   FontFamily="Consolas" />
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </local:TimelineItemTemplateSelector.EventTemplate>

        </local:TimelineItemTemplateSelector>

    </Page.Resources>

    <!-- ── Page root ──────────────────────────────────────────────────────── -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- header -->
            <RowDefinition Height="Auto" />   <!-- filter chips -->
            <RowDefinition Height="*" />      <!-- scrollable list -->
        </Grid.RowDefinitions>

        <!-- ── Header ────────────────────────────────────────────────────── -->
        <Grid Grid.Row="0" Padding="32,28,32,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Operational Timeline"
                           Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="Chronological record of system events, findings, and actions."
                           Style="{StaticResource CaptionTextStyle}" />
            </StackPanel>

            <!-- Live event count badge -->
            <Border Grid.Column="1"
                    Background="{StaticResource AppElevatedSurfaceBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="20"
                    Padding="14,6"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE823;"
                              FontFamily="Segoe MDL2 Assets"
                              FontSize="13"
                              Foreground="{StaticResource AccentBlueBrush}" />
                    <TextBlock>
                        <Run Text="{x:Bind ViewModel.TotalEventCount, Mode=OneWay}" />
                        <Run Text=" events" Foreground="{StaticResource SecondaryTextBrush}" />
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ── Filter chip strip ─────────────────────────────────────────── -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      Padding="32,18,32,0">
            <StackPanel Orientation="Horizontal" Spacing="8">

                <!-- Warnings chip -->
                <Button Command="{x:Bind ViewModel.ToggleWarningFilterCommand}"
                        Background="{x:Bind ViewModel.WarningChipBg, Mode=OneWay}"
                        BorderBrush="{x:Bind ViewModel.WarningChipBorder, Mode=OneWay}"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE7BA;"
                                  FontFamily="Segoe MDL2 Assets"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.WarningChipText, Mode=OneWay}" />
                        <TextBlock Text="Warnings"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.WarningChipText, Mode=OneWay}" />
                    </StackPanel>
                </Button>

                <!-- Recommendations chip -->
                <Button Command="{x:Bind ViewModel.ToggleRecommendationFilterCommand}"
                        Background="{x:Bind ViewModel.RecommendationChipBg, Mode=OneWay}"
                        BorderBrush="{x:Bind ViewModel.RecommendationChipBorder, Mode=OneWay}"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE946;"
                                  FontFamily="Segoe MDL2 Assets"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.RecommendationChipText, Mode=OneWay}" />
                        <TextBlock Text="Recommendations"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.RecommendationChipText, Mode=OneWay}" />
                    </StackPanel>
                </Button>

                <!-- Forecasts chip -->
                <Button Command="{x:Bind ViewModel.ToggleForecastFilterCommand}"
                        Background="{x:Bind ViewModel.ForecastChipBg, Mode=OneWay}"
                        BorderBrush="{x:Bind ViewModel.ForecastChipBorder, Mode=OneWay}"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE9D9;"
                                  FontFamily="Segoe MDL2 Assets"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.ForecastChipText, Mode=OneWay}" />
                        <TextBlock Text="Forecasts"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.ForecastChipText, Mode=OneWay}" />
                    </StackPanel>
                </Button>

                <!-- Actions chip -->
                <Button Command="{x:Bind ViewModel.ToggleActionFilterCommand}"
                        Background="{x:Bind ViewModel.ActionChipBg, Mode=OneWay}"
                        BorderBrush="{x:Bind ViewModel.ActionChipBorder, Mode=OneWay}"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE768;"
                                  FontFamily="Segoe MDL2 Assets"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.ActionChipText, Mode=OneWay}" />
                        <TextBlock Text="Actions"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.ActionChipText, Mode=OneWay}" />
                    </StackPanel>
                </Button>

                <!-- Session chip -->
                <Button Command="{x:Bind ViewModel.ToggleSessionFilterCommand}"
                        Background="{x:Bind ViewModel.SessionChipBg, Mode=OneWay}"
                        BorderBrush="{x:Bind ViewModel.SessionChipBorder, Mode=OneWay}"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE7B8;"
                                  FontFamily="Segoe MDL2 Assets"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.SessionChipText, Mode=OneWay}" />
                        <TextBlock Text="Session"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.SessionChipText, Mode=OneWay}" />
                    </StackPanel>
                </Button>

            </StackPanel>
        </ScrollViewer>

        <!-- ── Event list ─────────────────────────────────────────────────── -->
        <Grid Grid.Row="2" Margin="32,18,32,24">

            <!-- Empty state -->
            <StackPanel Visibility="{x:Bind ViewModel.EmptyStateVisibility, Mode=OneWay}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="12">
                <FontIcon Glyph="&#xE823;"
                          FontFamily="Segoe MDL2 Assets"
                          FontSize="48"
                          Foreground="{StaticResource SecondaryTextBrush}"
                          HorizontalAlignment="Center" />
                <TextBlock Text="No events yet"
                           Style="{StaticResource SectionTitleStyle}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Events appear here as the system is monitored."
                           Style="{StaticResource CaptionTextStyle}"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- Virtualized timeline list -->
            <ListView ItemsSource="{x:Bind ViewModel.FlatItems, Mode=OneWay}"
                      ItemTemplateSelector="{StaticResource TimelineTemplateSelector}"
                      Visibility="{x:Bind ViewModel.ListVisibility, Mode=OneWay}"
                      SelectionMode="None"
                      IsItemClickEnabled="False">

                <!-- Smooth entrance animation when page loads -->
                <ListView.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition />
                    </TransitionCollection>
                </ListView.Transitions>

                <!-- Strip the default ListViewItem chrome: remove selection highlight,
                     padding, and focus rectangles so cards render flush -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment"   Value="Stretch" />
                        <Setter Property="Padding"                    Value="0" />
                        <Setter Property="Margin"                     Value="0" />
                        <Setter Property="MinHeight"                  Value="0" />
                        <Setter Property="Background"                 Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

            </ListView>
        </Grid>

    </Grid>
</Page>
